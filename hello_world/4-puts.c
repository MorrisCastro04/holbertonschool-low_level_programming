#include <stdio.h>
#main is a special type funcion and is the start of the program, where the executions starts.

int main(void) {
	puts("\"Programming is like building a multilingual puzzle");
	getchar();
	return (0);
}
