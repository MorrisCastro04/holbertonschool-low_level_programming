#include <stdio.h>
/**
 *main: main start the program and return printf
 *description: printf will print "print with proper grammar, but the outcome is a piece of art,"
 *return: return 0 at the end
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
