#include <stdio.h>
/**
 *main - start the program and return printf
 *printf: will print "print with proper grammar, but the outcome is a piece of art,"
 *return: 0 at the end
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
