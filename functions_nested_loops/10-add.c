#include <stdio.h>
#include "main.h"
/**
 *add - sum two integers
 *Return: Return the result of the sum
 *@one: is the first integer to sum
 *@two: is the second integer to sum
 */

int add(int one, int two)
{
	int result;

	result = (one + two);
	return (result);
}
