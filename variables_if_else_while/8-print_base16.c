#include <stdio.h>
/**
 *
 *
 *
 */

int main(void)
{
	int n;
	char a_f;

	for (n = '0'; n <= '15'; n++)
	{
		putchar(n);
	} for (a_f = 'a'; a_f <= 'f'; a_f++)
	{ putchar(a_f);
	} putchar('\n');
	return (0);
}
