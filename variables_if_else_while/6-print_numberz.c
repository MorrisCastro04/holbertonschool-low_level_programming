#include <stdio.h>
/**
 *main - starts the program
 *description: print from 0 to 10 in chart
 *Return: Return 0 always
 *
 */

int main(void)
{ int n;

	for (n = '0'; n <= '9'; n++)
	{ putchar(n);
	}
	putchar('\n');
	return (0);
}
